#include <iostream>
#include <string>

using std::cout;
using std::endl;

int main()
{
    // Specify that a variable is an unsigned integer
    // Use auto to infer the data type from the value
    auto x = 5U;
    cout << x << endl;
}